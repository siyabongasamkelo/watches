import { Container } from "react-bootstrap";
import Cart from "../components/cart/Cart";
const CartPage = () => {
  return (
    <Container>
      <Cart />
    </Container>
  );
};

export default CartPage;
