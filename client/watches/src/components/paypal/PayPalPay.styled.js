import styled from "styled-components";
import { CheckOutCover, CheckoutStyled } from "../checkout/Checkout.styled";

export const PaypalPayStyled = styled(CheckoutStyled)``;

export const PaypaPayCover = styled(CheckOutCover)``;
