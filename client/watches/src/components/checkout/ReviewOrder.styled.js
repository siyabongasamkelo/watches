import styled from "styled-components";
import { BillingHeader } from "./BillingDetailsForm.styled";
import {
  CartSubTotalCover,
  CartTotal,
  CartTotalHeader,
} from "../cart/Cart.styled";

export const ReviewOrderStyled = styled.div``;

export const ReviewOrderHeader = styled(BillingHeader)``;

export const ReviewOrderCover = styled.div``;

export const ReviewTotalHeader = styled(CartTotalHeader)``;

export const ReviewTotalCover = styled(CartSubTotalCover)``;

export const ReviewTotal = styled(CartTotal)``;

export const PaymentMethodCover = styled.div``;
